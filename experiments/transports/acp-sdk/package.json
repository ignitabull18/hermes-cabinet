{
  "name": "hermes-cabinet-acp-sdk-probe",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/fixture-agent.mjs dist/src/fixture-agent.mjs",
    "test": "npm run build && node --test dist/test/*.test.js",
    "fixture": "npm run build && node dist/src/cli.js fixture",
    "live": "npm run build && node dist/src/cli.js live"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.3.0"
  },
  "devDependencies": {
    "@types/node": "22.15.3",
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
